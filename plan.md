# Robotiq 2F-85 + PTS 触觉抓取路线与原则

> 本文档的角色是“路线与原则”，不是“当前实现现状”。
>
> 当前仓库已经实现到哪一步、哪些能力已完成、哪些能力仍缺失，
> 请以 `docs/source/design/task_architecture.rst` 为准。

## 目的

本项目的目标是基于 MuJoCo / mjlab 搭建一个面向真实硬件约束的触觉抓取实验基线。重点不是把 Robotiq 2F-85 理想化为力控夹爪，而是围绕真实系统更接近的控制方式，建立一个可逐步扩展到 sim2real 的强化学习环境。

目标系统是：

> 基于阵列触觉反馈的 Robotiq 2F-85 限力位置闭环抓取控制。

策略根据触觉反馈决定是否继续闭合、保持或轻微松开夹爪，并尽量以较小接触力实现稳定抓持。

## 核心判断

### 2F-85 的控制抽象

Robotiq 2F-85 不应在本项目中建模为理想力伺服夹爪。更合理的抽象是：

- 位置命令
- 速度限制
- 力限制

因此，第一阶段策略动作不应是 `F_target`，而应是夹爪位置命令增量 `Δu`。这样更接近真实执行能力，也能避免策略利用仿真中并不存在的理想力控制。

### 触觉优先于内部真值

MuJoCo 可以提供大量内部接触真值，但最终可迁移策略应尽量依赖真实系统也能获得的观测：

- 局部触觉阵列
- 由触觉阵列聚合得到的力/力矩
- 由触觉时间序列构造的滑移代理
- 夹爪自身位置、速度与命令

MuJoCo 内部接触信息可以用于奖励、终止条件和调试，但不应成为最终策略的核心观测来源。

## 分阶段路线

### V0：全局 wrench baseline

第一阶段先验证基础闭环是否工作：

- 动作为一维连续 `Δu`
- 观测以左右指尖全局 force / torque 为主
- 目标是先跑通模型加载、动作控制、奖励和终止逻辑

这一阶段实现简单，适合暴露控制接口和任务定义中的基础问题。

### V1：3x3 法向触觉阵列

第二阶段引入左右指尖 `3x3` 触觉图：

- 每个指尖 9 个 taxel
- 观测以局部接触分布为主
- 策略开始利用接触中心、偏载和接触面积变化

这一阶段是最接近 Contactile PTS 价值主张的最小版本。

### V2：触觉阵列 + wrench + slip proxy

第三阶段在局部触觉基础上补充全局量与滑移代理：

- 保留 `3x3` 局部触觉
- 增加全局 wrench 作为辅助信息
- 引入基于触觉时间差分或压力中心变化的滑移代理

这一阶段用于形成更强的 sim2real 候选策略。

## 奖励与训练原则

奖励设计应服务于“稳定抓住且不过度用力”这一目标。高层原则如下：

- 鼓励维持稳定双侧接触
- 惩罚过大的接触力
- 惩罚无意义的持续闭合
- 惩罚抖动或高频动作
- 对掉落施加明确终止惩罚

训练路线采用课程式推进：

- 从固定形状、固定质量、固定摩擦开始
- 再逐步增加尺寸、质量、摩擦和姿态随机化
- 最后再引入噪声、延迟和更强扰动

## Sim2Real 原则

为控制 sim2real gap，后续实现应遵守这些约束：

- 不把 MuJoCo 内部接触真值直接喂给最终策略
- 不把 2F-85 改造成理想 force actuator
- 触觉观测尽量贴近真实 PTS 可提供的信息形态
- 逐步加入噪声、零偏、灵敏度差异、动作延迟和接触参数随机化

## 当前里程碑定义

当前阶段的成功标准不是完整 sim2real，而是先建立一个可靠的 tactile gripper baseline：

- MJCF 可稳定加载
- 传感器读数可解释
- 环境 API 稳定
- 能训练出一个基础抓取策略

实现细节、当前代码入口、已完成项 / 未完成项，不再堆在本文件里维护。
这些内容统一收口到 `docs/source/design/` 下的实现文档，尤其是：

- `task_architecture.rst`：当前实现现状总览
- `tactile_pipeline.rst`：触觉信号链路
- `reward_design.rst`：奖励与终止
- `control_pipeline.rst`：动作与训练配置
- `pts_taxel_scheme.rst`：PTS sphere-taxel 建模
